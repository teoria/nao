project_name: filterbuy-agent
databases:
- type: athena
  name: athena-prod
  include: []
  exclude: []
  accessors:
  - columns
  - description
  - preview
  s3_staging_dir: {{env("AWS_S3_STAGING_DIR")}}
  region_name: {{env("AWS_REGION")}}
  aws_access_key_id: {{env("AWS_ACCESS_KEY")}}
  aws_secret_access_key: {{env("AWS_SECRET_KEY")}}
repos: []
# llm:
#   provider: anthropic
#   api_key: {{env("ANTHROPIC_API_KEY")}}

